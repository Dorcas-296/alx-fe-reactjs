The application implements:

- Nested routes
- Dynamic routing
- Protected routes (authentication simulation)
- Fallback routing (404 handling)

The goal is to showcase how React Router can manage complex navigation structures in modern React applications.

---

## 🚀 Technologies Used

- React (Vite)
- react-router-dom